#include <stdio.h>
/**
* main: printh the string using prinft function
* Descripotion: Using the main(void) function
* A C program that prints exactly with proper grammar, but the outcome is a piece of art
* return: always give 0 as a return value
*/

int main(void)
{
   printf("with proper grammar, but the outcome is a piece of art,");
   return(0);
}
